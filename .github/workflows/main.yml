def add(x, y): return x + y
def divide(x, y): return x / y if y != 0 else "Error"
num1 = float(input("Enter first number: "))
num2 = float(input("Enter second number: "))
operation = input("Select (+, -, *, /): ")
if operation == '+': print(add(num1, num2))
